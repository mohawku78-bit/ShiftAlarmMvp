# CHANGELOG

## Notes
- Keep entries short: version, user-visible change, verification, remaining risk.
- Dates below use the local workspace date.

## Entries
- 2026-03-12 `v089`: Closed the post-v088 stabilization pass. Manual reschedule from Home and Editor step 3 now uses the same feedback path, the first-setup wizard regained step-0 close/back behavior and explicit select-then-next actions, and the 2-minute self-test message now shows the exact `HH:mm:ss` trigger time so real-device timing can be judged correctly. Verification: `:app:testDebugUnitTest` passed with `JAVA_HOME=C:\Program Files\Android\Android Studio1\jbr`; Samsung device smoke pass confirmed manual reschedule feedback, wizard recovery, and second-accurate 2-minute self-test behavior. Remaining risk: normal pattern-based next-alarm registration still needs one final real-device pass after returning from ad-hoc manual alarm states.
- 2026-03-12 `v088`: Finished the restore post-check contract so app-backup restore now records a pending reliability recheck, routes straight into the shared reliability center, and keeps Home plus Editor step 3 in action-needed until the user explicitly rechecks reliability. Verification: `:app:testDebugUnitTest` passed with `JAVA_HOME=C:\Program Files\Android\Android Studio1\jbr`; `:app:installDebug` passed; manual emulator sanity on `Medium_Phone_API_36.0(AVD) - 16` confirmed that a seeded restore-pending state opens the reliability center with the restore warning and that a completed restore state drops back to the real setup reason. Remaining risk: a full end-to-end file-picker restore pass on device is still worth one final product QA run.
- 2026-03-12 `v087`: Added trigger-history-backed watchdog self-healing and completed the manual wall-clock validation pass far enough to catch and fix a foreground recovery race. Shared reschedules now broadcast reliability-state changes, recovery/watchdog stores commit before UI refreshes, and `MainActivity` performs a short follow-up reliability refresh after `TIME_CHANGED`, `TIMEZONE_CHANGED`, and `DATE_CHANGED`. Verification: `:app:testDebugUnitTest` passed with `JAVA_HOME=C:\Program Files\Android\Android Studio1\jbr`; emulator validation passed on `Medium_Phone_API_36.0(AVD) - 16` for watchdog missed-alarm self-heal, `TIMEZONE_CHANGED`, and `TIME_SET`; natural DST boundary validation later passed for both spring-forward and fall-back without false watchdog incidents. Remaining risk: final real-device QA is still needed on a normal daily schedule, especially for OEM-specific battery behavior.
- 2026-03-12 `v086`: Added a visible-session wall-clock timeline so Home next-alarm and registered-next-alarm verification now invalidate on minute ticks plus `TIME_CHANGED`, `TIMEZONE_CHANGED`, and `DATE_CHANGED` while the app is open. `MainActivity` now owns a runtime wall-clock receiver and ticker, and the screen rekeys next-trigger calculations to the shared wall-clock snapshot instead of stale assumptions. Verification: `:app:testDebugUnitTest` passed with `JAVA_HOME=C:\Program Files\Android\Android Studio1\jbr`; added `WallClockTimelineTest`. Remaining risk: DST and timezone behavior still needed a manual boundary pass at this point.
- 2026-03-12 `v085`: Removed `USE_EXACT_ALARM`, moved exact permission handling onto a shared enabled-alarm rescheduler, and upgraded recovery plus reliability state so exact loss can degrade to inexact fallback without crashing while still surfacing action-needed in Home and Editor. Exact permission gain/loss, boot/time/timezone/package replace, restore reschedule, self-test, and snooze now share the same scheduling contract and recovery audit trail. Verification: `:app:testDebugUnitTest` passed with `JAVA_HOME=C:\Program Files\Android\Android Studio1\jbr`; manual emulator validation passed for exact permission `ignore -> degraded fallback recovery` and `allow -> exact recovery` on `Medium_Phone_API_36.0(AVD) - 16`. Remaining risk: timezone/DST reschedule and restore follow-up routing still needed a final device pass.
- 2026-03-12 `v084`: Finished the app backup/restore UX by parsing selected backup files into a preview/confirm dialog before any data replacement, upgraded export/import feedback to include alarm/preset/log counts, and cleared editor/exception/undo UI state while refreshing presets, logs, and reliability status after restore. Verification: `:app:testDebugUnitTest` passed with `JAVA_HOME=C:\Program Files\Android\Android Studio1\jbr`. Remaining risk: manual device restore scenarios still needed a final product pass.
- 2026-03-12 `v083`: Added a shared `ReliabilityCenterPolicy` and `ReliabilityCenterUiModel` so the Home banner and Editor step 3 now read the same summary, setup checklist, recent reliability lines, and battery hint. The Home banner compresses to prioritized recent lines, and Editor step 3 renders the shared summary plus actions without recomputing separate setup or overview state. Verification: `:app:testDebugUnitTest` passed with `JAVA_HOME=C:\Program Files\Android\Android Studio1\jbr`. Remaining risk: backup/recovery UX was still the next product-facing gap at this point.
- 2026-03-12 `v082`: Centralized editor interval-week normalization through `normalizeEditorPatternState`, routed editor draft/preset/duplicate flows through the same pattern-state helper, and unified single-date plus range shift overrides through shared override helpers so skip/add state stays normalized. Verification: `:app:testDebugUnitTest` passed with `JAVA_HOME=C:\Program Files\Android\Android Studio1\jbr`. Remaining risk: reliability overview polishing and any legacy preset import semantics stayed deferred.
- 2026-03-12 `v081`: Removed the repo-pinned Gradle JDK path so builds now use Android Studio JBR or an injected `JAVA_HOME`. Normalized editor, preset, and backup week-pattern handling around `intervalWeeks`, added long-rotation and backup normalization unit coverage, and closed the first-setup wizard regression pass with notification permission setup plus sturdier Compose waits. Verification: `:app:testDebugUnitTest` passed; `:app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.example.shiftalarmmvp.ui.ShiftSetupWizardTest` passed on `Medium_Phone_API_36.0(AVD) - 16`. Remaining risk: long-rotation override UX and model expansion remained open.
- 2026-03-11 `v081`: Localized the manufacturer battery guide catalog into Korean product copy while keeping the asset JSON ASCII-safe with Unicode escapes. The catalog still uses `Build.MANUFACTURER` plus `Build.BRAND`, and unsupported devices still fall back to the shared in-app Android guide. Verification: `:app:testDebugUnitTest`, `:app:assembleDebug`.
- 2026-03-11 `v080`: Strengthened battery guide matching by checking both `Build.MANUFACTURER` and `Build.BRAND`, so derived brands like POCO, Redmi, Honor, iQOO, and Pixel map more reliably to vendor guidance. Unsupported devices still fall back to the shared in-app Android battery guide. Repaired the corrupted battery guide catalog, added metadata fields (`last_verified_at`, `source`), and added catalog tests for brand aliases and fallback lookup. Verification: `:app:testDebugUnitTest`, `:app:assembleDebug`.
